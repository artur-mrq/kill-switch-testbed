# kill-switch-testbed

Sacrificial Railway app for testing the AI Hub kill switch against real
Railway (disconnect/reconnect, stop/restart). No dependencies, no build,
no env vars — binds PORT and answers everything with identifying JSON.
